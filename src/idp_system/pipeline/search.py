"""Semantic search placeholder."""


class SemanticSearchService:
    """Future FAISS semantic search component."""

    def search(self, query: str) -> list[dict[str, object]]:
        raise NotImplementedError("Semantic search will be implemented in a later phase.")
